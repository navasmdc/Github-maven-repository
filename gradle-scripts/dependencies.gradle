def _versions = [
        kotlin          : '1.2.71', // need to manually upgrade build.gradle `plugin {}` blocks
        hamcrest        : '1.3',
        mockito : '2.20.0',
]

def _libraries = [
        lang : [
                kotlinStdlib : "org.jetbrains.kotlin:kotlin-stdlib:${_versions.kotlin}",
        ],
        spoon : [
                runner : "com.squareup.spoon:spoon-runner:1.7.1"
        ],
        test : [
                junit : 'junit:junit:4.12',
                hamcrestAll : "org.hamcrest:hamcrest-all:${_versions.hamcrest}",
                mockitoCore     : "org.mockito:mockito-core:${_versions.mockito}",
                mockitoKotlin   : "com.nhaarman:mockito-kotlin:1.5.0"
        ]
]

ext {
    localVersions = _versions
    localLibraries = _libraries
}
